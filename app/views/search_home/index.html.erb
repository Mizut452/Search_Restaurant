<!DOCTYPE html>
<html>
    <head>
        <title>レストラン探します</title>
        <meta name="viewport" content="width=device-width,initial-scale=1">
        <%= csrf_meta_tags %>
        <%= csp_meta_tag %>
    </head>

    <body>
  <div class="container">
  
    <%= form_with url: search_path, method: :get, local: true do |f| %>
      <%= f.text_field :poke_code, class: "search_form", placeholder: "ポケモンの名前を入力してください" %>
    <% end %>
  </div>
    <%= yield %>
  </body>
</html>